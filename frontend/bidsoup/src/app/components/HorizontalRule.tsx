import styled from 'styled-components';
import { components } from '../utils/color';

const HorizontalRule = styled.div`
  height: 1px;
  background-color: ${components.border};
  margin: 10px 0;
`;

export default HorizontalRule;
